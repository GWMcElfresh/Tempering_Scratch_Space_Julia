# Julia artifacts
*.jl.cov
*.jl.mem
/docs/build/
/docs/site/

# Julia package build artifacts
/deps/deps.jl
/deps/build.log

# Stan compilation artifacts (avoid committing compiled Stan models)
stan_models/*.hpp
stan_models/*.o
stan_models/*.so
stan_models/*.dylib
stan_models/*.dll
stan_models/*.exe
stan_models/*_model.hpp

# Julia Manifest (not committed for library packages – regenerated by Pkg.instantiate())
Manifest.toml

# Julia temporary files
*.jl.bak

# OS
.DS_Store
Thumbs.db
